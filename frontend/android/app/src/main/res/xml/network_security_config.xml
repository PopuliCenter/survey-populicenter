<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- Default: TOLAK cleartext (HTTP). Server produksi (risetcenter.com) sudah HTTPS,
         jadi seluruh trafik publik wajib terenkripsi. -->
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>

    <!-- Pengecualian: izinkan cleartext HANYA ke server lokal saat development
         (emulator 10.0.2.2, LAN, localhost). -->
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="true">10.0.2.2</domain>
        <domain includeSubdomains="true">192.168.1.101</domain>
        <domain includeSubdomains="true">localhost</domain>
    </domain-config>
</network-security-config>
